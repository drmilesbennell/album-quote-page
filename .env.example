# Password for the /admin area. Without it the app falls back to "changeme".
ADMIN_PASSWORD=pick-a-strong-password

# Postgres connection string. On Vercel this is added automatically when you
# create a Neon database from the Storage tab. Leave unset for local use and
# the app stores data in .data/pglite instead.
# DATABASE_URL=postgres://...
